import React from 'react';
import Logpasscode from './logpassword'

const Retypeinterface = () => {
  return( 
     <div>  
        < Logpasscode />
        </div> );

};

export default Retypeinterface;