"use client";

import ThemeColor from './ThemeColor'; // Correctly import the component

const ThemeColourPage: React.FC = () => {
  return (
    <div>
      <ThemeColor />
    </div>
  );
};

export default ThemeColourPage;
